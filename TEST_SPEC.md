# TEST_SPEC.md — midashi-sanmenkyo

<!-- scaffold template v1.8.0 から展開(2026-08-19)。R のため実行規約を testthat に読み替える -->

## 実行規約

- テスト実行と `test_run` 記録は `python harness/testrun.py --loop <loop_id>` 経由を必須とする(HC-003 直移植)
- 照合・パースを含むテストは、書く前に**特殊値の挙動表**(NA/NaN/0/端点/形式方言)を作り、
  除外規則と共にテスト内コメントへ残す。時刻パースは format/tz 明示(hodo HC-001 直移植)
- 較正が要る数値(類似度閾値・G-04 の precision 基準)は**較正実験の実測を経てから固定**する。
  実験前に推測で書かない
- クラスタリングの「正解」は手ラベルペア集合(tests/fixtures/labeled_pairs.csv)が定義する。
  ラベルの変更は専用コミット + 理由記録

## オラクルの構造

1. **決定的計算の厳密一致** — 語彙指標・類似度は手計算フィクスチャと分数のまま一致(G-01/G-02)
2. **数理的不変量** — 類似度の対称性/自己 1/直交 0、クラスタ分割の網羅・非重複・決定論(G-02/G-03)
3. **較正ゲート** — 手ラベルペアへの precision/recall(閾値と基準は較正で固定)(G-04)
4. **台帳・決定論回帰** — hodo-hangenki と同一(G-05/G-06)

## ケース一覧

| ID | 対応要求 | 層 | ケース | 期待 |
|---|---|---|---|---|
| T-001 | F-02 | unit | 正規化: 全半角・空白・記号ゆらぎ | 定義した正規化表どおり(例をフィクスチャ化) |
| T-002 | F-03/G-02 | unit | バイグラム分解の手計算例 | 「首相が表明」→ {首相, 相が, が表, 表明} と厳密一致 |
| T-003 | G-02 | unit | コサイン類似度: 同一文字列 | 1(厳密) |
| T-004 | G-02 | unit | 共有バイグラムなしのペア | 0(厳密) |
| T-005 | G-02 | unit | 手計算ペア(重なり半分等) | 手計算値と厳密一致(導出コメント必須) |
| T-006 | G-02 | unit | 対称性 sim(a,b)=sim(b,a) | ランダム風フィクスチャ 20 組で厳密一致 |
| T-007 | F-03/G-03 | unit | 閾値グラフの連結成分 | 手作り 6 見出し(2 クラスタ+単独 1)の分割が期待どおり |
| T-008 | G-03 | unit | クラスタ分割の性質 | 全見出しがちょうど 1 クラスタに属す(網羅・非重複)・同一入力 2 回で同一出力 |
| T-009 | G-04/F-04 | validation | 手ラベルペア集合(同事象 ≥30・別事象 ≥30) | 較正で固定した閾値での precision/recall が固定基準以上 |
| T-010 | F-05/G-01 | unit | 体言止め率: 定義に基づく判定例(体言止め/用言止め/記号止め) | フィクスチャ全件で期待ラベル一致 |
| T-011 | F-05/G-01 | unit | 数字使用率・かぎ括弧率・見出し長 | 手計算フィクスチャと厳密一致(全半角数字とも数える等、定義を写す) |
| T-012 | F-05/G-01 | unit | 感情語率(辞書マッチ) | 辞書部分集合フィクスチャで一致件数が手数え通り。否定文脈は数えない等の規則は kokoro-graph の教訓に従い仕様化してから実装 |
| T-013 | F-01/G-05 | integration | 台帳整合(SHA256・単調・重複 0) | hodo-hangenki T-009/T-010 と同一規約 |
| T-014 | F-01 | unit | 各社 RSS パーサ(保存フィクスチャ XML、社ごと) | outlet/guid/link/title が期待どおり。ネットワーク非依存 |
| T-015 | F-06/G-06 | integration | out/: index+統計ページ・見出しリンクが配信元 URL・JS なし | DOM 規約 + `<script` なし + 見出し `<a href>` が pins の配信元ドメイン |
| T-016 | G-06 | integration | 同一入力 2 回レンダリング | バイト一致 + 回帰フィクスチャ |
| T-017 | F-06 | integration | 三面鏡クラスタの表示条件 | 2 社以上のクラスタのみ index に載る。1 社クラスタは載らない |

註: G-04 の較正は loop_002 で実データ収集後に実施(手ラベル→閾値スイープ→固定)。
それまで T-009 は「ラベル集合が存在しない」状態で fail させておく(skip にしない)。
