# SPEC.md — midashi-sanmenkyo(見出しの三面鏡)

<!-- scaffold template v1.8.0 から展開(2026-08-19)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

同じ出来事が、報道各社でどう違う見出しになるかを並べて見せる「三面鏡」。
複数社のニュース RSS を定時スナップショットし、同一事象の見出しをクラスタリングして
多社並置で表示する。あわせて各社の見出しの癖(体言止め率・数字使用率・感情語率・
かぎ括弧率・見出し長)を統計として可視化する。hodo-hangenki の観測インフラ
(Actions cron・不変スナップショット・SHA256 台帳)を多社に拡張したフリート 3 作目の R 観測所。

## 2. ドメインモデル

- **見出し** = (outlet, guid, title, link, 収集時刻)。同一性は outlet × guid
- **事象クラスタ** = 「同じ出来事を指す」と判定された見出しの集合(社をまたぐ)。
  文字バイグラムのコサイン類似度 ≥ 閾値の辺で結んだグラフの連結成分(閾値は較正で固定)
- **三面鏡クラスタ** = 2 社以上を含む事象クラスタ(表示の主役)
- **語彙指標** = 社ごとの見出し文体の統計。定義は §4 G-01 の通り決定的に計算できるもののみ

## 3. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 収集: 3 社の公開 RSS(loop_001 確定: **NHK cat0 主要**(RSS2.0)・**朝日 newsheadlines**(RSS1.0 RDF・guid なし→link 代用)・**毎日 mainichi-flash**(RDF)。共同=PR フィード・時事=ランキングのため不採用、産経・TBS は 404)を毎時スナップショット。保存は outlet・guid・link・title・収集時刻のみ(本文なし)。1 社の取得失敗では止めない | must |
| F-02 | 正規化: 見出しの全半角・空白・記号ゆらぎを決定的に正規化する純関数 | must |
| F-03 | 類似度: 文字バイグラムのコサイン類似度(純関数・辞書非依存)。クラスタリングは閾値グラフの連結成分 | must |
| F-04 | クラスタ判定の較正: 実データから作った**手ラベル付きペア集合**(同事象/別事象 各 30 ペア以上)で precision/recall を測り、閾値と合格基準(ゲート)を固定してから本線に使う。**loop_006 完了**(陽性 40 / 陰性 75、類似度帯で層化サンプリング)。ラベル規約: 同一事象 = 同じ具体的出来事(同じ発表・事故・判決・会見・試合結果)を主題とする。同一大事象の別展開(第一報 vs 会見、展望 vs 結果、A 社の行動 vs B 社の行動)は別事象 | must |
| F-05 | 語彙指標: 体言止め率・数字使用率・かぎ括弧使用率・感情語率(kokoro-graph の 419 語辞書を再利用)・平均見出し長。各定義は決定的に計算可能な形で仕様化する | must |
| F-06 | ページ: index(直近の三面鏡クラスタ一覧 — 多社並置カード)+ 社別統計ページ(指標比較)+ 定型注記。閲覧時 JS なし・静的 | must |
| F-07 | 自動更新: GitHub Actions cron(毎時)→ 収集 → クラスタ再計算 → 再レンダリング → コミット → Vercel Git 連携配信(hodo-hangenki 方式) | must |
| F-08 | フッタ: フリート標準 5 リンク(アーティファクト公開までは 3) | must |

## 4. 品質基準(ゲート)

`python harness/testrun.py --loop <id>`(testthat)が green であること。

| ゲート | 基準 |
|---|---|
| G-01 語彙指標の厳密性 | 各指標を手計算フィクスチャで厳密一致。感情語率は kokoro-graph 辞書の部分集合フィクスチャで検証 |
| G-02 類似度の数理 | コサイン類似度: 対称性・自己類似 1・直交 0 を厳密検証。手計算ペアと一致 |
| G-03 クラスタの構造 | 決定論(同一入力→同一クラスタ)・連結成分の性質(推移閉包・分割の網羅と非重複) |
| G-04 較正ゲート | **loop_006 で固定**: 類似度閾値 0.40(`R/params.R`)、ゲートは帯サイズ重み付けの推定精度 ≥ 0.90・条件付き再現 ≥ 0.40。較正実測(2026-08-22 / 49 スナップショット・524 ストーリー・社跨ぎ 24h 内 61,387 ペア・層化手ラベル 115 件)は 0.40 で精度 0.963 / 再現 0.496、0.30 で 0.707 / 0.668。三面鏡は誤並置が可視的に悪いため精度優先。緩和は再較正の証拠なしに不可 |
| G-05 台帳整合 | append-only スナップショット + SHA256 台帳(hodo-hangenki G-03 と同一。.gitattributes の EOL 保護込み) |
| G-06 描画決定論 | 同一入力 → SVG/HTML バイト一致 + 回帰フィクスチャ |

## 5. 運用・法務上の約束

- 見出しとリンクのみ保存・表示(本文・画像なし)。全ページに社名の出典と取得時刻を明記し、
  各見出しは配信元記事へのリンクにする(**HTML で表示するのでリンク必須** — SVG に埋めない)
- 並置は「同一事象の見出しの事実の対照」であり、優劣・正誤の評価語を UI に持ち込まない
- 各社のフィード利用条件を loop_001 で検分し pins に記録。条件が合わない社は使わない
- User-Agent にプロジェクト URL、収集間隔は 1 時間より密にしない

## 6. スコープ外 / 既知の限界

**既知の限界(loop_006 較正で観測)**: 文字バイグラム類似度は「同じ出来事だが語彙が
大きく異なる見出し」を捕まえられない。候補プール(類似度 0.15 未満)の外側にある陽性は
閾値をどう選んでも取れず、これは指標の限界であって閾値の問題ではない。したがって
G-04 の再現は**候補プール内の条件付き値**として測る(母集団の陽性総数は未知)。

### スコープ外

- 本文取得・要約・論調の自動評価(並置と決定的な語彙統計のみ)
- 形態素解析エンジン(MeCab 等)への依存 — 文字バイグラムと決定的規則で通す
- リアルタイム表示・検索(cron 周期の静的生成のみ)

## 7. アーキテクチャ方針

- hodo-hangenki の直移植: Actions cron → out/ コミット → Vercel Git 連携(vercel.json:
  buildCommand null / outputDirectory out)、不変スナップショット+台帳、testrun.py、
  .gitattributes の EOL 保護(初日から入れる — hodo loop_006 の教訓)
- `R/` は純関数のみ(IO・時刻・乱数禁止)。時刻は build/ が注入
- kokoro-graph の感情辞書は data/pins 系の固定コピーとして取り込む(参照先リポジトリの
  変更に引きずられない)
- 較正資産(手ラベルペア集合)は tests/fixtures に置き、変更は専用コミット + 理由記録
