# HARNESS_CHANGELOG.md — ハーネス改訂台帳(midashi-sanmenkyo)

原則: **エージェントがミスをするたびに、そのミスが二度と起きないようハーネスを改良する。**
起票条件: 同一失敗コード累計 2 回(LL-10)、または severity S1(LL-12)。

---

## HC-001

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-08-22 |
| トリガー | `TOOL-MISUSE` × 2(loop_006: `sample(idx, n)` の前置安定性を仮定して既ラベル 3 ペアが重複混入 / loop_008: 本番反映の待機条件を「スナップショット 52 回以上」と誤指定(実際は 51 回目)し `until` ループが 10 分空転) |
| 診断 | どちらも「自分で書いた抽出・待機の条件を、対象の実際の値と突き合わせずに確定した」。特に待機条件のマジックナンバーは、外れても**エラーにならず黙って空転する**ため損失が大きい |
| 改訂 | AGENTS.md §2 に追加: 「(a) 抽出・待機の条件を書く前に、対象の実際の値を 1 回出力して境界を確認する。(b) デプロイ反映の待機は手書きのマジックナンバーを使わず、**ローカル生成物と本番の一致**を条件にする(例: `diff <(curl -s $URL) out/index.html`)。(c) `until` ループには必ず上限時間を設ける」 |
| 種別 | agents_md |
| SCAFFOLD_VERSION | 1.8.0(プロジェクト局所) |
| 効果検証 | 以後 5 ループで TOOL-MISUSE の再発 0 件なら Closed |
| propagation | midashi-sanmenkyo ✅(本文書 + AGENTS.md §2)/ レジストリ還流 ⬜ |
| 状態 | Open |
