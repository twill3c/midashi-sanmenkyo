# 較正で固定したパラメータ(F-04/G-04)。
# **この値の変更は再較正(build/calibrate.R の実測)とセットでのみ許される**(AGENTS §3)。
#
# SIM_THRESHOLD: 事象クラスタを結ぶ文字バイグラム・コサイン類似度の閾値。
#   loop_006 較正(2026-08-22 / 49 スナップショット・524 ストーリー・
#   社跨ぎ 24h 内 61,387 ペア・層化手ラベル 115 件)の実測:
#     0.40 → 推定精度 0.963 / 条件付き再現 0.496
#     0.30 → 推定精度 0.707 / 条件付き再現 0.668
#   三面鏡は「同じ出来事の見出しを並べる」UI であり、誤並置が可視的に悪い。
#   よって精度優先で 0.40 を採用(ゲート: 精度 >= 0.90・再現 >= 0.40)。
SIM_THRESHOLD <- 0.40

# クラスタ計算の対象ウィンドウ(秒)。初出がこの範囲内の見出し同士のみ比較する。
CLUSTER_WINDOW_SEC <- 24 * 3600
